{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "scripts/deploy.sh"
  },
  "devDependencies": {
    "@types/ws": "8.5.4",
    "nuxt": "3.3.1"
  },
  "dependencies": {
    "ws": "8.13.0",
    "zod": "3.21.4"
  }
}
